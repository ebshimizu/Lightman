// lumiverse
#include "LumiverseCore.h"

using namespace Lumiverse;

typedef enum { DEVICE, PANEL } SampleMode;

/* create a sample of each device in the rig*/
void sample_rig(Rig rig, SampleMode mode);
